# Copy to .env and edit before `docker compose up`.

# Random ≥32 byte string. If empty, the API is unauthenticated (NOT for prod).
HT_REPRO_TOKEN=

# Host port the UI/API binds to
HT_REPRO_PORT=8765
